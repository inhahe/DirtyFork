import DirtyFork
import register_validator as Register # todo: should we include this information in a yaml somehow instead?

# i wonder if we should pass the values variable to all the functions, not just submit, so they don't have to get user.input_fields.*.contents to cross-validate inputs.

